# Phase 1 CLI: set DATABASE_URL in your shell (never commit credentials).
DATABASE_URL=
# Future web adapters; no remote services are called in Phase 1.
NEXT_PUBLIC_SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
AI_PROVIDER=mock
AI_PROVIDER_API_KEY=
IMAGE_PROVIDER=mock
IMAGE_PROVIDER_API_KEY=
ASSET_STORAGE_PROVIDER=local
APP_URL=http://localhost:3000
